type Product = {
  id: string;
  image: string;
  title: string;
  price: number;
  quantity: number;
  totalPrice: number;
  category: string;
  isAvailable: boolean;
  toWishlist: boolean;
};

export default Product;
